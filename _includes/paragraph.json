{% for paragraph in include.section.paragraphs %}
  {
    "id": "{{ include.section.id }}.{{ paragraph.id }}",
    "text": "{{ paragraph.text }}",
    "page": "{{ include.page }}"
  },
{% endfor %}
